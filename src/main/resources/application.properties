server.port=8080

spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB

# Directory where uploaded/converted/generated PDFs are stored (created if missing)
app.storage.dir=${java.io.tmpdir}/fillable-form-generator

# Override if soffice.exe is not on PATH / not in a default install location
app.libreoffice.path=
